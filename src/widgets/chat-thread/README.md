# widgets/chat-thread

Переиспользуемый компонент диалога — используется и для чата с людьми
(гид, поддержка), и для ИИ-агента. Разные источники данных подключаются
через хуки из `features/chat` и `features/ai-agent`.
